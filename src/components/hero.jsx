import React from 'react';

function Hero(props) {
    return (
        <section id="hero" className="d-flex justify-cntent-center align-items-center">
        </section>
    );
}

export default Hero;